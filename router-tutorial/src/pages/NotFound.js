import React from 'react';

const NotFound = () => {
    return (
        <div sytle={{
            dispaly : 'flex',
            alignItems : 'center',
            justifyContent : 'center',
            fontSize : 64,
            possition : 'absolute',
            width : '100%',
            height : '100%',
        }}
        >
            404
        </div>
    );
};

export default NotFound;